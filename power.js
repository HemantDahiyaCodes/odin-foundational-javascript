function power(num1, num2) {
    return num1 ** num2;
  }

  let num_1 = +prompt("Enter the value of first number:");
  let num_2 = +prompt("Enter the valueo f second number");

  console.log(power(num_1, num_2));